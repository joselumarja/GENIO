{
  "id": "hough_lines",
  "name": "Hough Lines",
  "category": "features",
  "description": "Detect lines using Hough transform.",
  "parameters": [
    {
      "name": "threshold",
      "token": "@threshold",
      "type": "number",
      "required": true,
      "description": "Threshold value."
    },
    {
      "name": "rho",
      "token": "@rho",
      "type": "number",
      "required": true,
      "description": "Distance resolution for Hough transform."
    },
    {
      "name": "theta",
      "token": "@theta",
      "type": "number",
      "required": true,
      "description": "Angle resolution for Hough transform."
    }
  ],
  "interface": {
    "inputs": [
      {
        "format": "image"
      }
    ],
    "outputs": [
      {
        "format": "array"
      }
    ],
    "compatibility": "An output can feed another stage input when format, and any declared dtype/channels constraints, are compatible."
  },
  "implementations": {
    "hls": {
      "vitis_vision": "implementations/hls/vitis_vision/hough_lines.json"
    },
    "functional": {
      "opencv": "implementations/functional/opencv/hough_lines.json"
    }
  }
}
