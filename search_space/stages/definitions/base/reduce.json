{
  "id": "reduce",
  "name": "Reduce",
  "category": "statistics",
  "description": "Reduce image along rows or columns.",
  "parameters": [
    {
      "name": "op",
      "token": "@op",
      "type": "enum",
      "required": true,
      "description": "Reduction operation."
    },
    {
      "name": "dim",
      "token": "@dim",
      "type": "integer",
      "required": true,
      "description": "Reduction dimension."
    }
  ],
  "interface": {
    "inputs": [
      {
        "format": "image"
      }
    ],
    "outputs": [
      {
        "format": "array"
      }
    ],
    "compatibility": "An output can feed another stage input when format, and any declared dtype/channels constraints, are compatible."
  },
  "implementations": {
    "hls": {
      "vitis_vision": "implementations/hls/vitis_vision/reduce.json"
    },
    "functional": {
      "opencv": "implementations/functional/opencv/reduce.json"
    }
  }
}
