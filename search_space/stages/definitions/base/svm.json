{
  "id": "svm",
  "name": "SVM",
  "category": "classification",
  "description": "Run support vector machine classification primitive.",
  "parameters": [
    {
      "name": "model",
      "token": "@model",
      "type": "object",
      "required": true,
      "description": "Preconfigured model object."
    }
  ],
  "interface": {
    "inputs": [
      {
        "format": "array"
      }
    ],
    "outputs": [
      {
        "format": "array"
      }
    ],
    "compatibility": "An output can feed another stage input when format, and any declared dtype/channels constraints, are compatible."
  },
  "implementations": {
    "hls": {
      "vitis_vision": "implementations/hls/vitis_vision/svm.json"
    },
    "functional": {
      "scikit_learn": "implementations/functional/scikit_learn/svm.json"
    }
  }
}
