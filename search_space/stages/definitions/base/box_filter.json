{
  "id": "box_filter",
  "name": "Box Filter",
  "category": "filtering",
  "description": "Apply normalized box filtering.",
  "parameters": [
    {
      "name": "kernel_rows",
      "token": "@K_ROWS",
      "type": "integer",
      "required": true,
      "description": "Kernel height."
    },
    {
      "name": "kernel_cols",
      "token": "@K_COLS",
      "type": "integer",
      "required": true,
      "description": "Kernel width."
    },
    {
      "name": "border_type",
      "token": "@BORDER_TYPE",
      "type": "enum",
      "required": true,
      "description": "Border handling policy."
    }
  ],
  "interface": {
    "inputs": [
      {
        "format": "image"
      }
    ],
    "outputs": [
      {
        "format": "image"
      }
    ],
    "compatibility": "An output can feed another stage input when format, and any declared dtype/channels constraints, are compatible."
  },
  "implementations": {
    "hls": {
      "vitis_vision": "implementations/hls/vitis_vision/box_filter.json"
    },
    "functional": {
      "opencv": "implementations/functional/opencv/box_filter.json"
    }
  }
}
