{
  "id": "accumulate_weighted",
  "name": "Accumulate Weighted",
  "category": "image_arithmetic",
  "description": "Compute weighted accumulation of an image.",
  "parameters": [
    {
      "name": "alpha",
      "token": "@alpha",
      "type": "number",
      "required": true,
      "description": "First scale or weight factor."
    }
  ],
  "interface": {
    "inputs": [
      {
        "format": "image"
      }
    ],
    "outputs": [
      {
        "format": "image"
      }
    ],
    "compatibility": "An output can feed another stage input when format, and any declared dtype/channels constraints, are compatible."
  },
  "implementations": {
    "hls": {
      "vitis_vision": "implementations/hls/vitis_vision/accumulate_weighted.json"
    },
    "functional": {
      "opencv": "implementations/functional/opencv/accumulate_weighted.json"
    }
  }
}
