{
  "id": "accumulate",
  "name": "Accumulate",
  "category": "image_arithmetic",
  "description": "Accumulate one image into an accumulator image.",
  "parameters": [],
  "interface": {
    "inputs": [
      {
        "format": "image"
      }
    ],
    "outputs": [
      {
        "format": "image"
      }
    ],
    "compatibility": "An output can feed another stage input when format, and any declared dtype/channels constraints, are compatible."
  },
  "implementations": {
    "hls": {
      "vitis_vision": "implementations/hls/vitis_vision/accumulate.json"
    },
    "functional": {
      "opencv": "implementations/functional/opencv/accumulate.json"
    }
  }
}
