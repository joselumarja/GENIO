{
  "id": "distance_transform",
  "name": "Distance Transform",
  "category": "segmentation",
  "description": "Compute distance transform.",
  "parameters": [
    {
      "name": "dist_type",
      "token": "@dist_type",
      "type": "enum",
      "required": true,
      "description": "Distance transform type."
    },
    {
      "name": "mask_size",
      "token": "@mask_size",
      "type": "integer",
      "required": true,
      "description": "Distance transform mask size."
    }
  ],
  "interface": {
    "inputs": [
      {
        "format": "image"
      }
    ],
    "outputs": [
      {
        "format": "image"
      }
    ],
    "compatibility": "An output can feed another stage input when format, and any declared dtype/channels constraints, are compatible."
  },
  "implementations": {
    "hls": {
      "vitis_vision": "implementations/hls/vitis_vision/distance_transform.json"
    },
    "functional": {
      "opencv": "implementations/functional/opencv/distance_transform.json"
    }
  }
}
