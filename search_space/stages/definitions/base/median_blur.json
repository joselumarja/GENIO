{
  "id": "median_blur",
  "name": "Median Blur",
  "category": "filtering",
  "description": "Apply median blur filtering.",
  "parameters": [
    {
      "name": "filter_width",
      "token": "@FILTER_WIDTH",
      "type": "integer",
      "required": true,
      "description": "Filter or kernel width."
    }
  ],
  "interface": {
    "inputs": [
      {
        "format": "image"
      }
    ],
    "outputs": [
      {
        "format": "image"
      }
    ],
    "compatibility": "An output can feed another stage input when format, and any declared dtype/channels constraints, are compatible."
  },
  "implementations": {
    "hls": {
      "vitis_vision": "implementations/hls/vitis_vision/median_blur.json"
    },
    "functional": {
      "opencv": "implementations/functional/opencv/median_blur.json"
    }
  }
}
