{
  "id": "multiply",
  "name": "Multiply",
  "category": "arithmetic",
  "description": "Compute per-pixel image multiplication.",
  "parameters": [],
  "interface": {
    "inputs": [
      {
        "format": "image"
      },
      {
        "format": "image"
      }
    ],
    "outputs": [
      {
        "format": "image"
      }
    ],
    "compatibility": "An output can feed another stage input when format, and any declared dtype/channels constraints, are compatible."
  },
  "implementations": {
    "hls": {
      "vitis_vision": "implementations/hls/vitis_vision/multiply.json"
    },
    "functional": {
      "opencv": "implementations/functional/opencv/multiply.json"
    }
  }
}
