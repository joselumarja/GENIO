{
  "id": "hdr_decompand",
  "name": "HDR Decompand",
  "category": "isp",
  "description": "Apply HDR decompanding.",
  "parameters": [
    {
      "name": "maxval",
      "token": "@maxval",
      "type": "number",
      "required": true,
      "description": "Maximum output value."
    },
    {
      "name": "gain",
      "token": "@gain",
      "type": "number",
      "required": true,
      "description": "Gain value."
    }
  ],
  "interface": {
    "inputs": [
      {
        "format": "image"
      }
    ],
    "outputs": [
      {
        "format": "image"
      }
    ],
    "compatibility": "An output can feed another stage input when format, and any declared dtype/channels constraints, are compatible."
  },
  "implementations": {
    "hls": {
      "vitis_vision": "implementations/hls/vitis_vision/hdr_decompand.json"
    },
    "functional": {
      "numpy": "implementations/functional/numpy/hdr_decompand.json"
    }
  }
}
