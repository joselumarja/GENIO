{
  "id": "custom_convolution",
  "name": "Custom Convolution",
  "category": "filtering",
  "description": "Apply custom convolution filter.",
  "parameters": [
    {
      "name": "border_type",
      "token": "@BORDER_TYPE",
      "type": "enum",
      "required": true,
      "description": "Border handling policy."
    }
  ],
  "interface": {
    "inputs": [
      {
        "format": "image"
      }
    ],
    "outputs": [
      {
        "format": "image"
      }
    ],
    "compatibility": "An output can feed another stage input when format, and any declared dtype/channels constraints, are compatible."
  },
  "implementations": {
    "hls": {
      "vitis_vision": "implementations/hls/vitis_vision/custom_convolution.json"
    },
    "functional": {
      "opencv": "implementations/functional/opencv/custom_convolution.json"
    }
  }
}
