{
  "id": "in_range",
  "name": "In Range",
  "category": "thresholding",
  "description": "Check whether image pixels lie between lower and upper bounds.",
  "parameters": [
    {
      "name": "lower_bound",
      "token": "@lower_bound",
      "type": "array",
      "required": true,
      "description": "Lower bound value or vector."
    },
    {
      "name": "upper_bound",
      "token": "@upper_bound",
      "type": "array",
      "required": true,
      "description": "Upper bound value or vector."
    }
  ],
  "interface": {
    "inputs": [
      {
        "format": "image"
      }
    ],
    "outputs": [
      {
        "format": "image"
      }
    ],
    "compatibility": "An output can feed another stage input when format, and any declared dtype/channels constraints, are compatible."
  },
  "implementations": {
    "hls": {
      "vitis_vision": "implementations/hls/vitis_vision/in_range.json"
    },
    "functional": {
      "opencv": "implementations/functional/opencv/in_range.json"
    }
  }
}
