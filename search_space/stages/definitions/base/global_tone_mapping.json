{
  "id": "global_tone_mapping",
  "name": "Global Tone Mapping",
  "category": "isp",
  "description": "Apply global tone mapping.",
  "parameters": [
    {
      "name": "maxval",
      "token": "@maxval",
      "type": "number",
      "required": true,
      "description": "Maximum output value."
    }
  ],
  "interface": {
    "inputs": [
      {
        "format": "image"
      }
    ],
    "outputs": [
      {
        "format": "image"
      }
    ],
    "compatibility": "An output can feed another stage input when format, and any declared dtype/channels constraints, are compatible."
  },
  "implementations": {
    "hls": {
      "vitis_vision": "implementations/hls/vitis_vision/global_tone_mapping.json"
    },
    "functional": {
      "numpy": "implementations/functional/numpy/global_tone_mapping.json"
    }
  }
}
