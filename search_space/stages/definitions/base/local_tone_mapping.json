{
  "id": "local_tone_mapping",
  "name": "Local Tone Mapping",
  "category": "isp",
  "description": "Apply local tone mapping.",
  "parameters": [
    {
      "name": "clip_limit",
      "token": "@clip_limit",
      "type": "number",
      "required": true,
      "description": "CLAHE contrast clipping limit."
    }
  ],
  "interface": {
    "inputs": [
      {
        "format": "image"
      }
    ],
    "outputs": [
      {
        "format": "image"
      }
    ],
    "compatibility": "An output can feed another stage input when format, and any declared dtype/channels constraints, are compatible."
  },
  "implementations": {
    "hls": {
      "vitis_vision": "implementations/hls/vitis_vision/local_tone_mapping.json"
    },
    "functional": {
      "scikit_image": "implementations/functional/scikit_image/local_tone_mapping.json"
    }
  }
}
