{
  "id": "gain_control_multi",
  "name": "Gain Control Multi",
  "category": "isp",
  "description": "Apply multi-channel gain control.",
  "parameters": [
    {
      "name": "maxval",
      "token": "@maxval",
      "type": "number",
      "required": true,
      "description": "Maximum output value."
    },
    {
      "name": "gains",
      "token": "@gains",
      "type": "array",
      "required": true,
      "description": "Per-channel gain values."
    }
  ],
  "interface": {
    "inputs": [
      {
        "format": "image"
      }
    ],
    "outputs": [
      {
        "format": "image"
      }
    ],
    "compatibility": "An output can feed another stage input when format, and any declared dtype/channels constraints, are compatible."
  },
  "implementations": {
    "hls": {
      "vitis_vision": "implementations/hls/vitis_vision/gain_control_multi.json"
    },
    "functional": {
      "numpy": "implementations/functional/numpy/gain_control_multi.json"
    }
  }
}
