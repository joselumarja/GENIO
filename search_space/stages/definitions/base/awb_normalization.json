{
  "id": "awb_normalization",
  "name": "AWB Normalization",
  "category": "isp",
  "description": "Normalize image for AWB pipeline.",
  "parameters": [
    {
      "name": "maxval",
      "token": "@maxval",
      "type": "number",
      "required": true,
      "description": "Maximum output value."
    }
  ],
  "interface": {
    "inputs": [
      {
        "format": "image"
      }
    ],
    "outputs": [
      {
        "format": "image"
      }
    ],
    "compatibility": "An output can feed another stage input when format, and any declared dtype/channels constraints, are compatible."
  },
  "implementations": {
    "hls": {
      "vitis_vision": "implementations/hls/vitis_vision/awb_normalization.json"
    },
    "functional": {
      "numpy": "implementations/functional/numpy/awb_normalization.json"
    }
  }
}
