{
  "stage": "resize",
  "imports": [
    "import cv2 as cv"
  ],
  "function": [
    "@output = cv.resize(@input, (@OUT_COLS, @OUT_ROWS), interpolation=@INTERPOLATION)"
  ],
  "configuration": [
    "@OUT_ROWS",
    "@OUT_COLS",
    "@INTERPOLATION"
  ],
  "tokens": {
    "@INTERPOLATION": "Interpolation policy resolved by the same configuration used by HLS composition.",
    "@OUT_COLS": "Output image width resolved by the same configuration used by HLS composition.",
    "@OUT_ROWS": "Output image height resolved by the same configuration used by HLS composition.",
    "@input": "Input image from the previous pipeline step.",
    "@output": "Output generated by this stage."
  },
  "source": "opencv"
}
