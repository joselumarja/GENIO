{
  "stage": "histogram",
  "imports": [
    "import cv2 as cv"
  ],
  "function": [
    "ranges = [@range_start, @range_end]",
    "@output_0 = cv.calcHist([@input_0], [@channel], @input_1, [@bins], ranges)"
  ],
  "configuration": [
    "@channel",
    "@bins",
    "@range_start",
    "@range_end"
  ],
  "tokens": {
    "@bins": "Histogram bins provided by the concrete test configuration.",
    "@channel": "Channel index provided by the concrete test configuration.",
    "@input_0": "Input image from the previous pipeline step.",
    "@input_1": "Mask image provided by the concrete pipeline configuration.",
    "@output_0": "Output generated by this stage.",
    "@range_start": "Histogram range lower bound.",
    "@range_end": "Histogram range upper bound."
  },
  "source": "opencv"
}
