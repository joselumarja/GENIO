{
  "stage": "dilate",
  "imports": [
    "import cv2 as cv"
  ],
  "function": [
    "kernel = cv.getStructuringElement(@K_SHAPE, (@K_COLS, @K_ROWS))",
    "@output = cv.dilate(@input, kernel, iterations=@ITERATIONS)"
  ],
  "configuration": [
    "@K_SHAPE",
    "@K_ROWS",
    "@K_COLS",
    "@ITERATIONS"
  ],
  "tokens": {
    "@ITERATIONS": "Number of morphology iterations resolved by the same configuration used by HLS composition.",
    "@K_COLS": "Kernel width resolved by the same configuration used by HLS composition.",
    "@K_ROWS": "Kernel height resolved by the same configuration used by HLS composition.",
    "@K_SHAPE": "Structuring element shape resolved by the same configuration used by HLS composition.",
    "@input": "Input image from the previous pipeline step.",
    "@output": "Output generated by this stage."
  },
  "source": "opencv"
}
