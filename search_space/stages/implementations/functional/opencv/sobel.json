{
  "stage": "sobel",
  "imports": [
    "import cv2 as cv"
  ],
  "function": [
    "@output_0 = cv.Sobel(@input_0, cv.CV_16S, 1, 0, ksize=@FILTER_WIDTH, borderType=@BORDER_TYPE)",
    "@output_1 = cv.Sobel(@input_0, cv.CV_16S, 0, 1, ksize=@FILTER_WIDTH, borderType=@BORDER_TYPE)"
  ],
  "configuration": [
    "@FILTER_WIDTH",
    "@BORDER_TYPE"
  ],
  "tokens": {
    "@BORDER_TYPE": "Border handling policy resolved by the same configuration used by HLS composition.",
    "@FILTER_WIDTH": "Filter/kernel width resolved by the same configuration used by HLS composition.",
    "@input_0": "Input image from the previous pipeline step.",
    "@output_0": "Horizontal derivative output generated by this stage.",
    "@output_1": "Vertical derivative output generated by this stage."
  },
  "source": "opencv"
}
