{
  "stage": "sobel",
  "imports": [
    "import cv2 as cv"
  ],
  "function": [
    "@output_x = cv.Sobel(@input, cv.CV_16S, 1, 0, ksize=@FILTER_WIDTH, borderType=@BORDER_TYPE)",
    "@output_y = cv.Sobel(@input, cv.CV_16S, 0, 1, ksize=@FILTER_WIDTH, borderType=@BORDER_TYPE)"
  ],
  "configuration": [
    "@FILTER_WIDTH",
    "@BORDER_TYPE"
  ],
  "tokens": {
    "@BORDER_TYPE": "Border handling policy resolved by the same configuration used by HLS composition.",
    "@FILTER_WIDTH": "Filter/kernel width resolved by the same configuration used by HLS composition.",
    "@input": "Input image from the previous pipeline step.",
    "@output_x": "Horizontal derivative output generated by this stage.",
    "@output_y": "Vertical derivative output generated by this stage."
  },
  "source": "opencv"
}
