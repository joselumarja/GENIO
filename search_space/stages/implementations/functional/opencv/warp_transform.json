{
  "stage": "warp_transform",
  "imports": [
    "import cv2 as cv"
  ],
  "function": [
    "@output = cv.warpPerspective(@input, @matrix, @dsize, flags=@INTERPOLATION, borderMode=@BORDER_TYPE)"
  ],
  "configuration": [
    "@INTERPOLATION",
    "@BORDER_TYPE",
    "@matrix",
    "@dsize"
  ],
  "tokens": {
    "@BORDER_TYPE": "Border handling policy resolved by the same configuration used by HLS composition.",
    "@INTERPOLATION": "Interpolation policy resolved by the same configuration used by HLS composition.",
    "@dsize": "Output size provided by the concrete test configuration.",
    "@input": "Input image from the previous pipeline step.",
    "@matrix": "Transform matrix provided by the concrete test configuration.",
    "@output": "Output generated by this stage."
  },
  "source": "opencv"
}
