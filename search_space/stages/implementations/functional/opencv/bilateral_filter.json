{
  "stage": "bilateral_filter",
  "imports": [
    "import cv2 as cv"
  ],
  "function": [
    "@output = cv.bilateralFilter(@input, @diameter, @sigma_color, @sigma_space, borderType=@BORDER_TYPE)"
  ],
  "configuration": [
    "@BORDER_TYPE",
    "@diameter",
    "@sigma_color",
    "@sigma_space"
  ],
  "tokens": {
    "@BORDER_TYPE": "Border handling policy resolved by the same configuration used by HLS composition.",
    "@diameter": "Bilateral filter diameter provided by the concrete test configuration.",
    "@input": "Input image from the previous pipeline step.",
    "@output": "Output generated by this stage.",
    "@sigma_color": "Bilateral filter sigmaColor provided by the concrete test configuration.",
    "@sigma_space": "Bilateral filter sigmaSpace provided by the concrete test configuration."
  },
  "source": "opencv"
}
