{
  "stage": "in_range",
  "imports": [
    "import cv2 as cv",
    "import numpy as np"
  ],
  "function": [
    "lower_bound = np.array([@lower_0, @lower_1, @lower_2])",
    "upper_bound = np.array([@upper_0, @upper_1, @upper_2])",
    "@output_0 = cv.inRange(@input_0, lower_bound, upper_bound)"
  ],
  "configuration": [
    "@lower_0",
    "@lower_1",
    "@lower_2",
    "@upper_0",
    "@upper_1",
    "@upper_2"
  ],
  "tokens": {
    "@input_0": "Input image from the previous pipeline step.",
    "@output_0": "Output generated by this stage.",
    "@lower_0": "Lower bound for channel 0.",
    "@lower_1": "Lower bound for channel 1.",
    "@lower_2": "Lower bound for channel 2.",
    "@upper_0": "Upper bound for channel 0.",
    "@upper_1": "Upper bound for channel 1.",
    "@upper_2": "Upper bound for channel 2."
  },
  "source": "opencv"
}
