{
  "stage": "median_blur",
  "imports": [
    "import cv2 as cv"
  ],
  "function": [
    "@output = cv.medianBlur(@input, @FILTER_WIDTH)"
  ],
  "configuration": [
    "@FILTER_WIDTH"
  ],
  "tokens": {
    "@FILTER_WIDTH": "Filter/kernel width resolved by the same configuration used by HLS composition.",
    "@input": "Input image from the previous pipeline step.",
    "@output": "Output generated by this stage."
  },
  "source": "opencv"
}
