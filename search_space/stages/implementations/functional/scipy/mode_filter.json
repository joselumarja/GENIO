{
  "stage": "mode_filter",
  "imports": [
    "from scipy import ndimage",
    "import numpy as np"
  ],
  "function": [
    "@output = ndimage.generic_filter(@input, lambda values: np.bincount(values.astype(np.int64)).argmax(), size=kernel_size)"
  ],
  "configuration": [
    "@kernel_size"
  ],
  "tokens": {
    "@input": "Input data from the previous pipeline step.",
    "@kernel_size": "Kernel size provided by the concrete test configuration.",
    "@output": "Output generated by this stage."
  },
  "source": "scipy"
}
