{
  "stage": "awb",
  "imports": [
    "import numpy as np"
  ],
  "function": [
    "means = np.mean(@input, axis=(0, 1), keepdims=True)",
    "gray = np.mean(means)",
    "@output = np.clip(@input.astype(np.float32) * (gray / np.maximum(means, 1e-6)), 0, @maxval).astype(@input.dtype)"
  ],
  "configuration": [
    "@maxval"
  ],
  "tokens": {
    "@input": "Input data from the previous pipeline step.",
    "@maxval": "Maximum data value provided by the concrete test configuration.",
    "@output": "Output generated by this stage."
  },
  "source": "numpy"
}
