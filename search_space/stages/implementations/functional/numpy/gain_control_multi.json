{
  "stage": "gain_control_multi",
  "imports": [
    "import numpy as np"
  ],
  "function": [
    "@output = np.clip(@input.astype(np.float32) * @gains, 0, @maxval).astype(@input.dtype)"
  ],
  "configuration": [
    "@maxval",
    "@gains"
  ],
  "tokens": {
    "@gains": "Per-channel gain values provided by the concrete test configuration.",
    "@input": "Input data from the previous pipeline step.",
    "@maxval": "Maximum data value provided by the concrete test configuration.",
    "@output": "Output generated by this stage."
  },
  "source": "numpy"
}
