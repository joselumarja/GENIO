{
  "stage": "awb_normalization",
  "imports": [
    "import numpy as np"
  ],
  "function": [
    "@output = np.clip(@input.astype(np.float32) / np.maximum(np.max(@input), 1e-6) * @maxval, 0, @maxval).astype(@input.dtype)"
  ],
  "configuration": [
    "@maxval"
  ],
  "tokens": {
    "@input": "Input data from the previous pipeline step.",
    "@maxval": "Maximum data value provided by the concrete test configuration.",
    "@output": "Output generated by this stage."
  },
  "source": "numpy"
}
