{
  "stage": "awb_normalization",
  "imports": [
    "import numpy as np"
  ],
  "function": [
    "@output_0 = np.clip(@input_0.astype(np.float32) / np.maximum(np.max(@input_0), 1e-6) * @maxval, 0, @maxval).astype(@input_0.dtype)"
  ],
  "configuration": [
    "@maxval"
  ],
  "tokens": {
    "@input_0": "Input data from the previous pipeline step.",
    "@maxval": "Maximum data value provided by the concrete test configuration.",
    "@output_0": "Output generated by this stage."
  },
  "source": "numpy"
}
