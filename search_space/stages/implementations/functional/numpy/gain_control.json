{
  "stage": "gain_control",
  "imports": [
    "import numpy as np"
  ],
  "function": [
    "@output = np.clip(@input.astype(np.float32) * @gain, 0, @maxval).astype(@input.dtype)"
  ],
  "configuration": [
    "@maxval",
    "@gain"
  ],
  "tokens": {
    "@gain": "Gain value provided by the concrete test configuration.",
    "@input": "Input data from the previous pipeline step.",
    "@maxval": "Maximum data value provided by the concrete test configuration.",
    "@output": "Output generated by this stage."
  },
  "source": "numpy"
}
