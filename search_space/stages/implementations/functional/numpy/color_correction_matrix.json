{
  "stage": "color_correction_matrix",
  "imports": [
    "import numpy as np"
  ],
  "function": [
    "@output = np.clip(@input @ @matrix + @bias, 0, @maxval).astype(@input.dtype)"
  ],
  "configuration": [
    "@maxval",
    "@matrix",
    "@bias"
  ],
  "tokens": {
    "@bias": "Bias vector provided by the concrete test configuration.",
    "@input": "Input data from the previous pipeline step.",
    "@matrix": "Transformation matrix provided by the concrete test configuration.",
    "@maxval": "Maximum data value provided by the concrete test configuration.",
    "@output": "Output generated by this stage."
  },
  "source": "numpy"
}
