{
  "stage": "gamma_correction",
  "imports": [
    "import numpy as np"
  ],
  "function": [
    "normalized = @input.astype(np.float32) / @maxval",
    "@output = np.clip(np.power(normalized, @gamma) * @maxval, 0, @maxval).astype(@input.dtype)"
  ],
  "configuration": [
    "@maxval",
    "@gamma"
  ],
  "tokens": {
    "@gamma": "Gamma value provided by the concrete test configuration.",
    "@input": "Input data from the previous pipeline step.",
    "@maxval": "Maximum data value provided by the concrete test configuration.",
    "@output": "Output generated by this stage."
  },
  "source": "numpy"
}
