{
  "stage": "black_level_correction",
  "imports": [
    "import numpy as np"
  ],
  "function": [
    "@output = np.clip(@input.astype(np.int32) - @black_level, 0, @maxval).astype(@input.dtype)"
  ],
  "configuration": [
    "@maxval",
    "@black_level"
  ],
  "tokens": {
    "@black_level": "Black level offset provided by the concrete test configuration.",
    "@input": "Input data from the previous pipeline step.",
    "@maxval": "Maximum data value provided by the concrete test configuration.",
    "@output": "Output generated by this stage."
  },
  "source": "numpy"
}
