{
  "stage": "bilateral_filter",
  "include": "imgproc/xf_bilateral_filter.hpp",
  "function": [
    "xf::cv::bilateralFilter<@diameter, @BORDER_TYPE, @TYPE, @ROWS, @COLS, @NPC>(@input_0, @output_0, @sigma_color, @sigma_space);"
  ],
  "configuration": [
    "@diameter",
    "@BORDER_TYPE",
    "@sigma_color",
    "@sigma_space"
  ],
  "tokens": {
    "@BORDER_TYPE": "Border handling policy.",
    "@COLS": "Maximum image width used to specialize the hardware function.",
    "@NPC": "Pixels processed per clock cycle selected by the composition layer.",
    "@ROWS": "Maximum image height used to specialize the hardware function.",
    "@TYPE": "Vitis Vision input/output pixel type selected by the composition layer.",
    "@diameter": "Bilateral filter window diameter.",
    "@input_0": "Input stream or matrix generated by the previous pipeline step.",
    "@output_0": "Output stream or matrix consumed by the next pipeline step.",
    "@sigma_color": "Bilateral filter sigmaColor.",
    "@sigma_space": "Bilateral filter sigmaSpace."
  },
  "source": "vision/L1/include/imgproc/xf_bilateral_filter.hpp"
}
