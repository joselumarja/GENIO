{
  "stage": "box_filter",
  "include": "imgproc/xf_box_filter.hpp",
  "function": [
    "xf::cv::boxFilter<@TYPE, @ROWS, @COLS, @NPC>(@input, @output);"
  ],
  "configuration": [
    "@K_ROWS",
    "@K_COLS",
    "@BORDER_TYPE"
  ],
  "tokens": {
    "@COLS": "Maximum image width used to specialize the hardware function.",
    "@NPC": "Pixels processed per clock cycle selected by the composition layer.",
    "@ROWS": "Maximum image height used to specialize the hardware function.",
    "@TYPE": "Vitis Vision input/output pixel type selected by the composition layer.",
    "@input": "Input stream or matrix generated by the previous pipeline step.",
    "@output": "Output stream or matrix consumed by the next pipeline step.",
    "@K_ROWS": "Kernel height resolved by the same configuration used by HLS composition.",
    "@K_COLS": "Kernel width resolved by the same configuration used by HLS composition.",
    "@BORDER_TYPE": "Border handling policy resolved by the same configuration used by HLS composition."
  },
  "source": "Vitis_Libraries/vision/L1/include/imgproc/xf_box_filter.hpp"
}
