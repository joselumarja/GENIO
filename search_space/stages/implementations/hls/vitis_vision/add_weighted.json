{
  "stage": "add_weighted",
  "include": "imgproc/xf_add_weighted.hpp",
  "function": [
    "xf::cv::addWeighted<@TYPE, @ROWS, @COLS, @NPC>(@input, @output);"
  ],
  "configuration": [
    "@alpha",
    "@beta",
    "@gamma"
  ],
  "tokens": {
    "@COLS": "Maximum image width used to specialize the hardware function.",
    "@NPC": "Pixels processed per clock cycle selected by the composition layer.",
    "@ROWS": "Maximum image height used to specialize the hardware function.",
    "@TYPE": "Vitis Vision input/output pixel type selected by the composition layer.",
    "@input": "Input stream or matrix generated by the previous pipeline step.",
    "@output": "Output stream or matrix consumed by the next pipeline step.",
    "@alpha": "Weight or scale factor provided by the concrete test configuration.",
    "@beta": "Weight or scale factor provided by the concrete test configuration.",
    "@gamma": "Bias term provided by the concrete test configuration."
  },
  "source": "Vitis_Libraries/vision/L1/include/imgproc/xf_add_weighted.hpp"
}
