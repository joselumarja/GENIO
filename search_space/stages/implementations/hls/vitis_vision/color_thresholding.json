{
  "stage": "color_thresholding",
  "include": "imgproc/xf_colorthresholding.hpp",
  "function": [
    "xf::cv::colorthresholding<XF_8UC3, XF_8UC1, @MAXCOLORS, @ROWS, @COLS, @NPC>(@input, @output, @low_thresh, @high_thresh);"
  ],
  "configuration": [
    "@low_thresh",
    "@high_thresh"
  ],
  "tokens": {
    "@COLS": "Maximum image width used to specialize the hardware function.",
    "@MAXCOLORS": "Maximum number of colors represented in threshold arrays.",
    "@NPC": "Pixels processed per clock cycle selected by the composition layer.",
    "@ROWS": "Maximum image height used to specialize the hardware function.",
    "@high_thresh": "Upper threshold vector provided by the concrete test configuration.",
    "@input": "Input stream or matrix generated by the previous pipeline step.",
    "@low_thresh": "Lower threshold vector provided by the concrete test configuration.",
    "@output": "Output stream or matrix consumed by the next pipeline step."
  },
  "source": "Vitis_Libraries/vision/L1/include/imgproc/xf_colorthresholding.hpp"
}
