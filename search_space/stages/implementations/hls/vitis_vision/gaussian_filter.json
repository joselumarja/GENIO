{
  "stage": "gaussian_filter",
  "include": "imgproc/xf_gaussian_filter.hpp",
  "function": [
    "xf::cv::GaussianBlur<@FILTER_WIDTH, @BORDER_TYPE, @TYPE, @ROWS, @COLS, @NPC>(@input, @output);"
  ],
  "configuration": [
    "@FILTER_WIDTH",
    "@BORDER_TYPE",
    "@sigma_x",
    "@sigma_y"
  ],
  "tokens": {
    "@BORDER_TYPE": "Border handling policy selected by the composition layer.",
    "@COLS": "Maximum image width used to specialize the hardware function.",
    "@FILTER_WIDTH": "Filter/kernel width selected by the composition layer.",
    "@NPC": "Pixels processed per clock cycle selected by the composition layer.",
    "@ROWS": "Maximum image height used to specialize the hardware function.",
    "@TYPE": "Vitis Vision input/output pixel type selected by the composition layer.",
    "@input": "Input stream or matrix generated by the previous pipeline step.",
    "@output": "Output stream or matrix consumed by the next pipeline step.",
    "@sigma_x": "Gaussian sigma in X direction provided by the concrete test configuration.",
    "@sigma_y": "Gaussian sigma in Y direction provided by the concrete test configuration."
  },
  "source": "Vitis_Libraries/vision/L1/include/imgproc/xf_gaussian_filter.hpp"
}
