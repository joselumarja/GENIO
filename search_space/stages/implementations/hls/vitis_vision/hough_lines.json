{
  "stage": "hough_lines",
  "include": "imgproc/xf_houghlines.hpp",
  "function": [
    "xf::cv::HoughLines<@TYPE, @ROWS, @COLS, @NPC>(@input, @output);"
  ],
  "configuration": [
    "@threshold",
    "@rho",
    "@theta"
  ],
  "tokens": {
    "@COLS": "Maximum image width used to specialize the hardware function.",
    "@NPC": "Pixels processed per clock cycle selected by the composition layer.",
    "@ROWS": "Maximum image height used to specialize the hardware function.",
    "@TYPE": "Vitis Vision input/output pixel type selected by the composition layer.",
    "@input": "Input stream or matrix generated by the previous pipeline step.",
    "@output": "Output stream or matrix consumed by the next pipeline step.",
    "@threshold": "Threshold value provided by the concrete test configuration.",
    "@rho": "Composition token resolved by the functional composition layer.",
    "@theta": "Composition token resolved by the functional composition layer."
  },
  "source": "Vitis_Libraries/vision/L1/include/imgproc/xf_houghlines.hpp"
}
