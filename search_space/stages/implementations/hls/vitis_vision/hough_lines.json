{
  "stage": "hough_lines",
  "include": "imgproc/xf_houghlines.hpp",
  "function": [
    "xf::cv::HoughLines<@RHO, @THETA, @MAXLINES, @DIAG, @MINTHETA, @MAXTHETA, @TYPE, @ROWS, @COLS, @NPC>(@input_0, @output_0, @output_1, @threshold, @linesmax);"
  ],
  "configuration": [
    "@threshold",
    "@RHO",
    "@THETA",
    "@linesmax"
  ],
  "tokens": {
    "@COLS": "Maximum image width used to specialize the hardware function.",
    "@DIAG": "Configuration value.",
    "@MAXLINES": "Configuration value.",
    "@MAXTHETA": "Configuration value.",
    "@MINTHETA": "Configuration value.",
    "@NPC": "Pixels processed per clock cycle selected by the composition layer.",
    "@RHO": "Distance resolution for Hough transform.",
    "@ROWS": "Maximum image height used to specialize the hardware function.",
    "@THETA": "Angle resolution for Hough transform.",
    "@TYPE": "Vitis Vision input/output pixel type selected by the composition layer.",
    "@input_0": "Input stream or matrix generated by the previous pipeline step.",
    "@linesmax": "Maximum number of lines to return.",
    "@output_0": "Configuration value.",
    "@output_1": "Configuration value.",
    "@threshold": "Threshold value."
  },
  "source": "vision/L1/include/imgproc/xf_houghlines.hpp"
}
