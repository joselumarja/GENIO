{
  "stage": "morph_close",
  "include": [
    "imgproc/xf_erosion.hpp",
    "imgproc/xf_dilation.hpp"
  ],
  "function": [
    "xf::cv::Mat<@TYPE, @ROWS, @COLS, @NPC> imgAux(@ROWS, @COLS);",
    "unsigned char kernel[K_ROWS * K_COLS];",
    "xf::cv::dilate<@BORDER_TYPE, @TYPE, @ROWS, @COLS, @K_SHAPE, @K_ROWS, @K_COLS, 1, @NPC>(@input_0, imgAux, kernel);",
    "xf::cv::erode<@BORDER_TYPE, @TYPE, @ROWS, @COLS, @K_SHAPE, @K_ROWS, @K_COLS, 1, @NPC>(imgAux, @output_0, kernel);"
    
  ],
  "configuration": [
    "@K_SHAPE",
    "@K_ROWS",
    "@K_COLS"
  ],
  "tokens": {
    "@BORDER_TYPE": "Border handling policy selected by the composition layer.",
    "@COLS": "Maximum image width used to specialize the hardware function.",
    "@K_COLS": "Kernel width selected by the composition layer.",
    "@K_ROWS": "Kernel height selected by the composition layer.",
    "@K_SHAPE": "Structuring element shape selected by the composition layer.",
    "@NPC": "Pixels processed per clock cycle selected by the composition layer.",
    "@ROWS": "Maximum image height used to specialize the hardware function.",
    "@TYPE": "Vitis Vision input/output pixel type selected by the composition layer.",
    "@input_0": "Input stream or matrix generated by the previous pipeline step.",
    "@kernel": "Kernel buffer generated by the composition layer.",
    "@output_0": "Output stream or matrix consumed by the next pipeline step."
  },
  "source": "vision/L1/include/imgproc/xf_erosion.hpp"
}
