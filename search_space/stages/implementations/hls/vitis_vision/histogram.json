{
  "stage": "histogram",
  "include": "imgproc/xf_histogram.hpp",
  "function": [
    "xf::cv::calcHist<@TYPE, @ROWS, @COLS, @NPC>(@input, @output);"
  ],
  "configuration": [
    "@channel",
    "@mask",
    "@bins",
    "@ranges"
  ],
  "tokens": {
    "@COLS": "Maximum image width used to specialize the hardware function.",
    "@NPC": "Pixels processed per clock cycle selected by the composition layer.",
    "@ROWS": "Maximum image height used to specialize the hardware function.",
    "@TYPE": "Vitis Vision input/output pixel type selected by the composition layer.",
    "@input": "Input stream or matrix generated by the previous pipeline step.",
    "@output": "Output stream or matrix consumed by the next pipeline step.",
    "@channel": "Channel index provided by the concrete test configuration.",
    "@mask": "Mask image provided by the concrete test configuration.",
    "@bins": "Histogram bins provided by the concrete test configuration.",
    "@ranges": "Histogram ranges provided by the concrete test configuration."
  },
  "source": "Vitis_Libraries/vision/L1/include/imgproc/xf_histogram.hpp"
}
