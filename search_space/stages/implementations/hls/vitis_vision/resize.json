{
  "stage": "resize",
  "include": "imgproc/xf_resize.hpp",
  "function": [
    "xf::cv::resize<@INTERPOLATION, @TYPE, @ROWS, @COLS, @OUT_ROWS, @OUT_COLS, @NPC, @USE_URAM, @MAXDOWNSCALE>(@input_0, @output_0);"
  ],
  "configuration": [
    "@OUT_ROWS",
    "@OUT_COLS",
    "@INTERPOLATION"
  ],
  "tokens": {
    "@COLS": "Maximum image width used to specialize the hardware function.",
    "@INTERPOLATION": "Interpolation policy selected by the composition layer.",
    "@MAXDOWNSCALE": "Maximum downscale factor selected by the composition layer.",
    "@NPC": "Pixels processed per clock cycle selected by the composition layer.",
    "@ROWS": "Maximum image height used to specialize the hardware function.",
    "@TYPE": "Vitis Vision input/output pixel type selected by the composition layer.",
    "@USE_URAM": "URAM usage flag selected by the composition layer.",
    "@input_0": "Input stream or matrix generated by the previous pipeline step.",
    "@output_0": "Output stream or matrix consumed by the next pipeline step.",
    "@OUT_ROWS": "Output image height provided by the concrete test configuration.",
    "@OUT_COLS": "Output image width provided by the concrete test configuration."
  },
  "source": "vision/L1/include/imgproc/xf_resize.hpp"
}
